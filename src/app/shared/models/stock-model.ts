class Stock{
  // sempre com a primeira letra dos tipos minuscula
  id: number;
  name: string;
  price: number;
  minimum: number;
  exclusiveMinimum: boolean;
  variation: number;
  date: string;
  //list: string[]; // Array
  //stock: stock;
}

export default Stock;
